import { StickyFloatDirective } from './sticky-float.directive';

describe('StickyFloatDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyFloatDirective();
    expect(directive).toBeTruthy();
  });
});
