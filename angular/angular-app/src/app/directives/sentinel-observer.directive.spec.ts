import { SentinelObserverDirective } from './sentinel-observer.directive';

describe('SentinelObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new SentinelObserverDirective();
    expect(directive).toBeTruthy();
  });
});
